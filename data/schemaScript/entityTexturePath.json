{
	"type": "enum",
	"generateFile": "clientEntity/dynamic/texturePathEnum.json",
	"script": "return readdir('RP/textures/entity').then(dirents => dirents.map(dirent => dirent.path.replace('RP/', '').replace(/\\.png|\\.jpeg|\\.jpg|\\.tga/g, '')))"
}
