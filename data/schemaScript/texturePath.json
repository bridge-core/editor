{
	"type": "enum",
	"generateFile": "texture/dynamic/texturePathEnum.json",
	"script": "return readdir('RP/textures').then(dirents => dirents.map(dirent => dirent.path.replace('RP/', '').replace(\\.png|\\.jpeg|\\.jpg|\\.tga/g, '')))"
}
